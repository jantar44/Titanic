from setuptools import setup

setup(
   name='titanic',
   version='0.1',
   description='Analysis of what sorts of people were likely to survive on titanic using ML',
   author='Me',
   packages=['titanic'],
   install_requires=['pandas', 'numpy', 'matplotlib','sklearn'],
